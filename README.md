##

```
# Install dependencies
# For ubuntu
sudo apt install librdkafka-dev

# For MACOS
brew install librdkafka pkg-config

### NOTE
###
# If it does not work this way. Try to remove the install libraries and install from source.
```
